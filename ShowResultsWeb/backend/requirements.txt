fastapi
uvicorn
python-multipart
ultralytics
PyYAML
pillow
torch
torchvision
torchmetrics
psutil

# 模型格式匯出（ONNX）。版本界線對齊 ultralytics exporter 的 check_requirements，
# 避免它在執行期認為版本不符（YOLO_AUTOINSTALL 已關閉，不會自動補裝）。
onnx>=1.12.0,<2.0.0
onnxruntime>=1.20.0
onnxslim>=0.1.82
